import { RatingWidthPipe } from './rating-width.pipe';

describe('RatingWidthPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingWidthPipe();
    expect(pipe).toBeTruthy();
  });
});
